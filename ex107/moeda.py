def metade (x):
    half = x / 2
    return half
def dobro(x):
    double = x * 2
    return double
def aumentar(x, y = 10):
    increase = x + (x * (y / 100))
    return increase
def diminuir(x, y = 10):
    low = x - (x * (y / 100))
    return low
